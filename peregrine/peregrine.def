Bootstrap: localimage
From: {{GUIX_SQUASHFS}}

%post
mkdir -p /opt/src
cd /opt/src
git clone https://github.com/eggert/tz
cd tz
CC=gcc make install

Rscript -e "devtools::install_github('doserjef/spOccupancy')"

%labels
author Kyle Shannon <kyle@pobox.com>
github.com/bsurc/container git rev={{GIT_REV}}
